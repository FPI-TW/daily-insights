# Replace this example during provisioning with the complete current list from
# https://www.cloudflare.com/ips/ and keep it updated. Never add 0.0.0.0/0 or
# ::/0: that would trust client-supplied CF-Connecting-IP headers.
set_real_ip_from 192.0.2.0/24;
set_real_ip_from 2001:db8::/32;

